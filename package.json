{
  "name": "receipts-suite",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:apps": "npm ci --include=dev --prefix apps/binance-withdraw && npm ci --include=dev --prefix apps/binance-deposit",
    "typecheck": "npm run typecheck --prefix apps/binance-withdraw && npm run typecheck --prefix apps/binance-deposit",
    "build": "npm run build --prefix apps/binance-withdraw && npm run build --prefix apps/binance-deposit",
    "preview": "node scripts/local-preview.mjs"
  }
}
